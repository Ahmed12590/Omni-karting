const swiper = new Swiper('.mySwiper', {
    autoplay: {
        delay: 3000, // Time in ms between slides
        disableOnInteraction: false, // Keeps autoplay even when the user interacts with the slider
    },
    loop: true, // Loop through slides
});


